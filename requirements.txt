# FastAPI and Web Dependencies
fastapi>=0.104.1,<0.110.0
uvicorn[standard]>=0.24.0,<0.30.0
python-multipart>=0.0.6,<0.1.0
pydantic>=2.4.0,<2.7.0

# PyTorch CPU-only (for optional model loading)
torch>=2.0.0,<2.3.0 --index-url https://download.pytorch.org/whl/cpu

# ML Dependencies (optional - will gracefully fail if not available)
transformers>=4.35.0,<4.40.0
datasets>=2.14.0,<2.20.0
accelerate>=0.24.0,<0.30.0

# Data Processing
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<1.27.0

# Additional dependencies for stability
Jinja2>=3.1.2
requests>=2.31.0

# Note: The app will work with fallback responses even if ML dependencies fail to install
